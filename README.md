# RL-Solutions-LLC

Wilmington DE general contractor marketing site + guided project estimator.

## App source

The Vite/React application lives in [`project/`](./project/).

```bash
cd project
cp .env.example .env   # or use existing Supabase keys
npm install
npm run dev
```

See the Phase 1 estimator → Supabase integration notes in the open PR for Admin RLS and Resend setup.
